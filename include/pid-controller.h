/*
class PID {
public:
    double kP, kI, kD;            // PID gains
    double tolerance;             // Error tolerance to stop
    double maxOutput;             // Maximum output value (e.g., motor velocity)
    double prevError;             // Previous error for calculating the derivative
    double integral;              // Accumulated integral for the I term

    PID(double kp, double ki, double kd, double tolerance = 1.0, double maxOutput = 127);

    double calculate(double error);
};
*/